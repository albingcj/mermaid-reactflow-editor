// mermaidParser: removed/disabled in this branch.
// The application now uses the editor (`mermaidSource`) as the single source of truth.
// This file intentionally exports nothing and is a harmless stub so the repository
// retains historical files without providing runtime behavior.

export {}; // no-op